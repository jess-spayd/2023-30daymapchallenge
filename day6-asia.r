{"metadata":{"kernelspec":{"name":"ir","display_name":"R","language":"R"},"language_info":{"name":"R","codemirror_mode":"r","pygments_lexer":"r","mimetype":"text/x-r-source","file_extension":".r","version":"4.0.5"}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"library(sf)\nlibrary(tidyverse)\nlibrary(maps)\nlibrary(mapdata)\nlibrary(mapproj)\nlibrary(geojsonio)\nlibrary(broom)","metadata":{"_uuid":"051d70d956493feee0c6d64651c6a088724dca2a","_execution_state":"idle","execution":{"iopub.status.busy":"2023-11-09T00:45:59.131441Z","iopub.execute_input":"2023-11-09T00:45:59.133849Z","iopub.status.idle":"2023-11-09T00:45:59.213692Z"},"trusted":true},"execution_count":11,"outputs":[]},{"cell_type":"code","source":"setwd('/kaggle/input/japan-rail')","metadata":{"execution":{"iopub.status.busy":"2023-11-09T00:43:26.798396Z","iopub.execute_input":"2023-11-09T00:43:26.803138Z","iopub.status.idle":"2023-11-09T00:43:26.821041Z"},"trusted":true},"execution_count":4,"outputs":[]},{"cell_type":"code","source":"# Load dataset\n\n\njp_rail <- geojson_read('/kaggle/input/japan-rail/N02-22_RailroadSection.geojson',\n                     what = \"sp\")\n","metadata":{"execution":{"iopub.status.busy":"2023-11-09T00:45:22.619725Z","iopub.execute_input":"2023-11-09T00:45:22.621281Z","iopub.status.idle":"2023-11-09T00:45:33.110344Z"},"trusted":true},"execution_count":10,"outputs":[]},{"cell_type":"code","source":"# View data\n\njp_rail_fortified <- tidy(jp_rail)\n\nggplot() +\n  geom_polygon(data = jp_rail_fortified, aes( x = long, y = lat, group = group), fill=\"#69b3a2\", color=\"white\") +\n  theme_void() +\n  coord_map()","metadata":{"execution":{"iopub.status.busy":"2023-11-09T00:46:21.165453Z","iopub.execute_input":"2023-11-09T00:46:21.167796Z","iopub.status.idle":"2023-11-09T00:54:04.223858Z"},"trusted":true},"execution_count":12,"outputs":[{"name":"stderr","text":"Warning message:\n“\u001b[1m\u001b[22m`tidy.SpatialLinesDataFrame()` was deprecated in broom 1.0.4.\n\u001b[36mℹ\u001b[39m Please use functions from the sf package, namely `sf::st_as_sf()`, in favor\n  of sp tidiers.”\nERROR while rich displaying an object: Error in grid.newpage(): could not open file 'Rplot001.png'\n\nTraceback:\n1. tryCatch(withCallingHandlers({\n .     if (!mime %in% names(repr::mime2repr)) \n .         stop(\"No repr_* for mimetype \", mime, \" in repr::mime2repr\")\n .     rpr <- repr::mime2repr[[mime]](obj)\n .     if (is.null(rpr)) \n .         return(NULL)\n .     prepare_content(is.raw(rpr), rpr)\n . }, error = error_handler), error = outer_handler)\n2. tryCatchList(expr, classes, parentenv, handlers)\n3. tryCatchOne(expr, names, parentenv, handlers[[1L]])\n4. doTryCatch(return(expr), name, parentenv, handler)\n5. withCallingHandlers({\n .     if (!mime %in% names(repr::mime2repr)) \n .         stop(\"No repr_* for mimetype \", mime, \" in repr::mime2repr\")\n .     rpr <- repr::mime2repr[[mime]](obj)\n .     if (is.null(rpr)) \n .         return(NULL)\n .     prepare_content(is.raw(rpr), rpr)\n . }, error = error_handler)\n6. repr::mime2repr[[mime]](obj)\n7. repr_text.default(obj)\n8. paste(capture.output(print(obj)), collapse = \"\\n\")\n9. capture.output(print(obj))\n10. evalVis(expr)\n11. withVisible(eval(expr, pf))\n12. eval(expr, pf)\n13. eval(expr, pf)\n14. print(obj)\n15. print.ggplot(obj)\n16. grid.newpage()\n","output_type":"stream"}]},{"cell_type":"code","source":"# Load Japan base map\n\njapan <- map_data('world')[map_data('world')$region == \"Japan\",]\n\nggplot(japan, aes(long, lat, group=group)) +\n  geom_polygon(color='black', fill='white') +\n  coord_fixed(1.3) +\n  theme(axis.title.x=element_blank(), axis.text.x=element_blank(), axis.ticks.x=element_blank(),\n        axis.title.y=element_blank(), axis.text.y=element_blank(), axis.ticks.y=element_blank()) +\n  theme(panel.background = element_blank())","metadata":{},"execution_count":null,"outputs":[]}]}